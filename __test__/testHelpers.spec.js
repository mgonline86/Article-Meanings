import { switchForm } from "../src/client/js/helpers"
   
describe("Testing the switch option functionality", () => {
    test("Testing the switchForm() function", () => {
           expect(switchForm).toBeDefined();
})});

import { onBlur } from "../src/client/js/helpers"
   
describe("Testing the onBlur from input functionality", () => {
    test("Testing the onBlur() function", () => {
           expect(onBlur).toBeDefined();
})});